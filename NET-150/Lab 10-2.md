## Goal:
The goal of this lab was to use packet tracer to reinforce our knowledge of processes and packet tracer.

## Useful Commands:
On a switch CLI tab, if you type "enable" and then "show mac-address-table", you can see the table of the mac addresses and ports they're connected to on the switch. On PCs if you go in the command prompt and type "arp -a" it'll show the arp table.

## Problems:
I had no problems
